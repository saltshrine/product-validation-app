@let(classes = 'mt-2 block text-sm text-[#c2413a]')
@if($context.hasErrors)
  <div {{ $props.merge({ id: `${$context.id}-error`, class: classes }).toAttrs() }}>
    @each(message in $context.errors)
      <span>{{ message }}</span>
    @end
  </div>
@end
